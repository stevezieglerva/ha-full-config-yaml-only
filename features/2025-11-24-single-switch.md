Implement a single virtual switch for indoor christmas lights that just stays on of off instead of the the existing switches that trigger on or off and get reset. Duplicate a single virtual switch for outdoor christmas lights

Remove the automations to reset the christmas virtual switches after 5 seconds.